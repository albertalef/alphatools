import { styled } from "@renderer/config/stitches.config"

export default function TraitBidInstanceContainer(){

    return (
        <Wrapper>
            
        </Wrapper>
    )
}

const Wrapper = styled('section', {
    display: "flex",
    flexGrow: 1,
    padding: '20px 12px',
    flexDirection: 'column',
    alignItems: 'center',
    gap: 14,
})